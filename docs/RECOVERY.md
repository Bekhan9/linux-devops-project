# Git Recovery Process

This project demonstrates recovery from an incorrect change.

Git keeps the complete development history, which allows problematic changes to be identified and safely reverted.

The git revert command creates a new commit that reverses an earlier change without deleting the existing commit history.
